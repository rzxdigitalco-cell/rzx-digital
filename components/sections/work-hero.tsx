import { PageHero } from "@/components/shared/page-hero";

export function WorkHero() {
  return (
    <PageHero
      eyebrow="Our Work"
      title="Projects Built With Purpose."
      description="A look at the websites we've shipped for real businesses across hospitality, healthcare, e-commerce, corporate, and restaurant clients — plus a couple of projects we built ourselves to sharpen the craft."
    />
  );
}
